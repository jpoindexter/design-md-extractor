// Coverage rendering uses setList() inline in the form handler — no dedicated render functions needed.

export function coverageCss(): string {
  return '';
}

export function coverageJs(): string {
  return '';
}
